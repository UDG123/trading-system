from __future__ import annotations

from typing import Sequence


def kalman_placeholder(series: Sequence[float]) -> list[float]:
    return list(series)


def wavelet_placeholder(series: Sequence[float]) -> list[float]:
    return list(series)


def savgol_placeholder(series: Sequence[float]) -> list[float]:
    return list(series)
